# Installation Instructions
- To install the package, use the following command:
  
pip install -r requirements.txt

# Upgrade Instructions
- To upgrade the package, use the command:
  
pip install --upgrade -r requirements.txt

... other content ...